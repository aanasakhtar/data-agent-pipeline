# Advisory Catalog Registry (empty — placeholder)

Not populated in this pilot. `executive-synthesis` (L11) is supposed to map
a verified finding to an existing advisory/solution construct rather than
inventing one. With this registry empty, L11 will correctly report
`advisory_match: "not applicable"` for now instead of fabricating a
solution recommendation — see the L11 skill's example output.

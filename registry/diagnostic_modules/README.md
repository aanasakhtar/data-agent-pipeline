# Diagnostic Modules Registry (empty — placeholder)

Not populated as a formal registry in this pilot. The diagnostic families
`operational-diagnosis` (L7) actually runs against this synthetic dataset
are listed inline in `.claude/skills/operational-diagnosis/SKILL.md`
("Diagnostic library" table), rather than as separate registry entries here.
Worth formalizing into this registry once there's more than one dataset to
diagnose against.

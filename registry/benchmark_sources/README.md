# Benchmark Sources Registry (empty — placeholder)

Not populated in this pilot. `framework-benchmark-intelligence` (L9) only
uses internal cohorts computed directly from this same synthetic dataset
(see the skill's example `BenchmarkRecord` with `benchmark_source: "internal"`).
No external benchmark dataset is loaded here.

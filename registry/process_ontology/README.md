# Process Ontology Registry (empty — placeholder)

Not populated in this pilot. In the full CLaiMB design this would hold the
canonical process taxonomy (e.g. an APQC PCF mapping) that `intent-business-scope`
(L4) and `kpi-metric-resolution` (L5) consult to resolve business terminology.
For this local pilot, L4/L5 work directly off the synthetic schema's table/column
names instead — fine for one dataset, but this registry is exactly what
would need real content before onboarding a second, differently-shaped
customer dataset.
